# Step X：功能名称

> 复制本文件后删除本提示。只写已经实现和验证的事实；规划放在“下一步”。

## 本阶段状态

- 状态：未开始 / 下一步 / 已完成 / 后置可选
- 完成日期：YYYY-MM-DD
- 对应开发 Step：
- 对应 Agent Phase：前置 / Phase 0 / Phase 1 / Phase 2 / Phase 3 / Phase 4 可选
- 对应需求或 Issue：
- 对应 ADR 与契约：

状态必须由代码、测试、评测或真实验收支撑。目录已创建、接口已设计或文档已写均不能作为“已完成”的证据。

## 要解决的问题

用一个具体用户场景说明输入、预期输出，以及本阶段刻意不解决什么。

## 架构位置

画出本阶段涉及的进程、Context、Port、Adapter 和外部存储。标明同步调用、异步事件与依赖方向。

## 完成了什么

列出用户可观察能力、领域规则、接口/事件/特征变化和失败处理。避免只罗列文件。

## 与 Agent 主线的关系

说明本切片属于 Agent 前置、Runtime、Search Agent、可靠性平台还是后置推荐深化；明确 Agent 是否已经存在。本节还要说明 Direct Search/确定性回退是否保持独立，以及 Tool 只能经过哪个业务输入 Port。

## 核心流程

用时序图或编号描述正常路径，并补充至少一个失败或降级路径。

## 关键代码入口

| 入口 | 作用 | 建议阅读顺序 |
| --- | --- | --- |
| `path/to/file` |  | 1 |

## 设计取舍

说明为什么采用当前方案、考虑过哪些替代方案、代价是什么。重大且长期有效的决定应另写 ADR 并在这里链接。

## 如何验证

提供可以复制执行的环境准备、单元测试、集成测试和手工验收命令，并写明预期结果。若需要固定数据集，也记录版本、生成方式、索引/策略/Prompt/模型版本和评测输出位置。

## 完成证据

列出实现入口、自动化测试、固定评测、真实 API 或故障注入记录。必须能据此复现结论，不能只提供页面截图或主观体验。

## 本阶段可以学到什么

解释关键概念如何映射到本项目代码，不要只给术语定义。

## 练习与自检问题

至少包含一个读代码练习、一个小改动练习和一个设计思考题。

## 常见问题与排查

记录真实遇到的问题、可观察现象、定位方法和解决方案。

## 下一步

只列尚未完成的内容、依赖条件和进入下一阶段的验收门槛。若本文是历史切片，同时写清“当时的下一步”和学习路线首页所定义的“当前下一步”，避免旧文档覆盖现行路线。
