# SeekFlux Agent 工作规则

本文件适用于整个仓库。执行代码、配置、契约或架构修改时，除完成实现和验证外，还必须维护与代码事实一致的学习文档。

## 每轮交付后的文档同步

每完成一轮具有实际交付结果的工作，都要在结束前检查 `docs/learning/`。这里的“一轮”是指代码、配置、契约、架构决定、测试或验收状态发生了变化；仅回答问题、只读排查或没有产生项目事实变化时，不要为了形式改文档。

按以下顺序处理：

1. 先读取 `docs/learning/README.md`，确认当前开发 Step、对应 Agent Phase、已完成能力和下一步。
2. 更新本轮对应的唯一 `step-NN-*.md`，记录已经实现并验证的事实、关键入口、失败/降级语义、验证命令、完成证据及剩余边界。
3. 如果本轮改变了阶段状态，再更新 `docs/learning/README.md` 中的当前 Step、路线表、下一步范围和完成门槛；不能只更新 Step 文档而留下总入口过期。
4. 只有真正开始新的 Step 时，才从 `docs/learning/template.md` 创建下一个按序文件；规划、目录预留和接口草图不构成“开始”或“完成”。
5. 长期架构决定同步进入 `docs/adr/`，API/事件/特征变化同步进入 `contracts/`，Agent/检索效果与基线同步进入 `evals/`；学习文档只链接这些事实来源，不复制整份内容。
6. 完成后检查 Markdown 本地链接、Step 编号、状态用词和 `git diff --check`，并在最终回复中说明更新了哪些学习文档。

## 学习文档的唯一职责

- `docs/learning/README.md`：唯一的进度与路线入口，只在这里维护“当前在哪一步、下一步是什么”。
- `docs/learning/step-NN-*.md`：按实现顺序记录一个纵向切片；历史文档保留当时事实，但其末尾必须链接总入口，不能宣称旧的下一步仍是当前任务。
- `docs/learning/template.md`：只用于创建新的 Step 文档，不记录项目进度。
- `SeekFlux.md`：目标产品和长期系统设计；`docs/module-boundaries.md`：依赖边界；`docs/adr/`：长期决策。不要在 `docs/learning/` 再创建重复的架构总览、路线副本、临时总结或同一 Step 的多份文档。

如果两份学习文档表达相同内容，保留上述职责中指定的事实来源，合并必要信息并删除重复文件。文件名使用连续的两位编号：`step-00-...md`、`step-01-...md`、`step-02-...md`。

## 状态与提交约束

学习路线中的状态只使用：`已完成`、`下一步`、`未开始`、`后置可选`。只有代码、自动化测试以及真实验收或固定评测共同支撑时，才能标记为“已完成”；不得把目标架构写成当前能力。

除非用户明确要求，不执行 `git add`、`git commit`、`git push`，也不替用户提交修改。
